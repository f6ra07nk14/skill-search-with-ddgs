[project]
name = "search-with-ddgs-installer-metadata"
version = "0.1.0"
description = "Install metadata for the search-with-ddgs skill installer."
requires-python = ">=3.10"
dependencies = [
  "ddgs[api,mcp]",
]
